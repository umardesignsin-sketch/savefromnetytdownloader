services:
  - type: web
    name: savefromnetytdownloader
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 5000
    disk:
      name: ytdl-data
      mountPath: /data
      sizeGB: 1
